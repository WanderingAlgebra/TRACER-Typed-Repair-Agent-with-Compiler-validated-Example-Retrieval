# TRACER 补丁记录

本文件记录影响安全性、实验可复现性、Lean 编译边界和公开发布的补丁。它与 `PROGRESS.md` 的职责不同：`PROGRESS.md` 描述当前状态，本文按提交批次记录变更原因、影响范围和验证证据。

## 2026-08-26 — 安全与发布验收补丁

对应提交：`be7b0e2`（`修复安全边界与实验发布验收问题`）。

### 修复内容

- Provider 只允许同来源重定向，错误正文脱敏 API Key。
- Lean 子进程改用最小环境变量；候选中的显式元编程、文件/进程 IO 和未完成证明公理会被拒绝。
- 编译结果将 `sorryAx` 和未完成证明警告视为失败。
- 定理定位严格匹配完全限定名称，支持 `lakefile.toml` 与 `lakefile.lean` 项目，并保留目标前的局部定义。
- A/B/C 评测缓存纳入轮次和实验批次；`--fresh` 清理 SQLite 主文件及旁车文件；报告禁止合并不同 `run_id` 或 `experiment_id`。
- 成本未配置时记录为 `null` / “未配置”，不再伪装成零成本。
- Capsule 回放脚本支持从仓库根目录或 Capsule 目录执行；发布审计扫描 Capsule 根目录孤立文件、`auth.json` 和敏感路径。
- PowerShell 脚本统一使用 UTF-8 BOM；Mathlib 准备脚本检查 Lake 退出码并设置缓存目录。
- 条件 C 排除与评测命题完全相同的检索示例，避免把原题完整答案误当作检索增益。
- 人工复核门槛要求覆盖当前实验的全部题目×条件组合；系统不会自动填写研究者判断。

### 验证证据

- Python 自动化测试：49/49 通过。
- `lake build`：通过。
- Capsule gallery 审计：24/24 通过。
- `evaluation18` Capsule 从仓库根目录和 Capsule 目录回放均通过。
- 仓库公开文本未发现 API Key 模式。

### 已知边界

- 环境变量清理和候选静态拒绝不是操作系统级沙箱；运行不受信任项目时仍应使用容器或低权限环境。
- `results/manual_review.csv` 的 54 条研究者复核必须由人工完成，未完成前不得发布正式 A/B/C 结论。

## 2026-08-26 — 吸收 leiteng 的试验发布流程

### 修复内容

- 为 Lean 编译子进程增加临时 HOME、TMP、APPDATA 和 LOCALAPPDATA，候选编译不再使用调用者的用户目录。
- 新增 `scripts/validate_pilot.py`，严格校验冻结的 54 个题目×条件组合、连续轮次、统一模型配置、候选策略和基础设施错误。
- `evaluate.py --fresh` 现在先可恢复归档旧日志、证明、复核台账和报告；只有显式复用缓存时才保留缓存。
- `report.py` 增加 formal/draft 门禁；新增 `scripts/export_pilot.py`，导出原始逐轮轨迹、成功证明和报告，并清理本机路径与认证信息。
- 导出清单仅记录相对路径和文件大小，不生成任何摘要或指纹字段；未伪造缺失的真实 provider 轨迹。

### 验证证据

- 已补充候选安全策略、检索泄漏预检和失败后重试链路；完整测试、Lean 构建和 capsule 回放需在当前机器工具链可用后执行。

### 已知边界

- 严格报告和导出要求真实 provider 的完整轨迹以及研究者填写的人工复核；mock/smoke 记录会被拒绝为正式结果。

## 记录规范

后续补丁至少应说明：

1. 变更日期与提交标识；
2. 修复的问题及其影响范围；
3. 是否改变实验协议、日志字段或公开文件格式；
4. 可复现的测试、构建或回放证据；
5. 尚未解决的安全或研究边界。
